version: '3.8'

services:
  tournament-manager:
    build: .
    ports:
      - "8081:8081"
    volumes:
      - ./local_storage:/app/local_storage
    environment:
      - PORT=8081
      - GOOGLE_OAUTH_CLIENT_ID=1040110950776-lp97tkf0bhc36c8s3v405c46hhf0m53m.apps.googleusercontent.com
    restart: always
